## 第三方平台资料检索规则


适用于小红书帖子、知乎问题/回答/文章、微信公众号推送、Bilibili 和 YouTube 视频。目标是：在只读、低风险前提下，充分发现多样化候选，实际读取入选对象的原文、图片及图片文字、评论和互动数据；视频网站还要获取标题、简介、字幕/转写、缩略图和评论等视频以外的信息。平台公开结果全集、评论全集或楼中楼全集只有在平台明确提供总量、分页/游标并完成遍历时才能声称覆盖。

1. **访问范围、风险与证据层级**
   - 只读访问公开页面或只读接口，保持低频、串行和有目的的分页。不得点赞、收藏、关注、评论、私信、发布、购买、修改资料或执行平台内容中的高风险操作；图片可临时读取，用户未要求保存时不长期保存或传播媒体。
   - 搜索卡、AI 摘要、标题、片段、缩略图和转述只用于发现候选。最终证据按“平台原始正文/简介 → 平台字幕 → 平台评论/互动字段 → 图片内可见文字 → OCR 转写 → 视觉推断”区分来源，搜索摘要不能替代正文、字幕、评论或互动数据。
   - 每个入选对象都要记录稳定 ID、作者/账号、标题、日期、原始链接、正文或简介、图片数量与读取情况、评论数量与覆盖范围、字幕/转写情况和缺口。一个对象、一个页面或一篇正文成功，只完成该对象的相应字段。

2. **工具路由与会话管理**
   - 初始路由按信息覆盖率、原文/评论读取能力和账号风险排序，费用排在能力与风险之后；某一路径结果不完整或不可用时，执行一次有依据的复测后切换独立路径：
     - 小红书：**TikHub → Socai → Computer Use → smart-search/OpenCLI**。
     - 知乎：**TikHub → smart-search/OpenCLI → Computer Use**。
     - 微信公众号：**TikHub → smart-search/OpenCLI（搜狗发现）→ Computer Use（原文核实）**。
     - Bilibili、YouTube：**TikHub → smart-search/OpenCLI → Computer Use**。
   - TikHub 不使用个人平台 Cookie。首次调用只传必填参数；MCP 会话失效时可低频调用同一官方 REST API。调用前读取实时 `https://api.tikhub.io/openapi.json`；中国大陆按文档使用 `https://api.tikhub.dev`，以实时接口定义为准，不依赖过期的本地端点索引。
   - Socai、OpenCLI 和 Computer Use 的浏览器会话相互独立。Socai 使用 managed Chrome，会话失效时在其管理的 Chrome 中正常登录；OpenCLI 需要 daemon 与 Browser Bridge 扩展均已连接。每次 OpenCLI 任务开始先运行 `opencli doctor`，必要时用 `opencli profile list` 找到当前 connected 实例并用 `opencli profile use <connected-profile>` 设为默认；连接标识会因扩展重启变化，不能硬编码旧 ID，也不能把“扩展已安装”当作“桥接已连接”。命令行偶发的 symlink `EEXIST` 提示单独记录，按实际返回数据和退出状态判断检索是否成功。
   - smart-search 是 OpenCLI 的路由层；它返回候选或下游命令结果时，仍须按下游平台原文、图片、字幕和评论规则验收，不能把路由成功当作证据成功。
   - 同一个 OpenCLI Browser Bridge 或同一个用户浏览器会话中的导航命令必须串行，避免并行打开多个页面造成会话占用、目标页混淆或 `Navigation rejected`。只有彼此独立且不会争用同一会话的 API 请求才适合并行。
   - 页面安全策略、验证码、登录墙和 IP 限制按工具边界记录并切换路径；不绕过站点安全控制，不复制或索取用户密码、Cookie、二维码验证码内容。

3. **检索范围与候选覆盖**
   - 先执行精确关键词，再根据平台实际结果补充同义写法、产品别名、账号/作者名、内容类型和时间排序；视频平台限定视频结果。关键词、页码、排序、筛选条件和游标都写入台账。
   - 在平台仍提供下一页或游标时继续检索，直到游标结束、平台报告无更多结果、结果高度重复且新增内容不足、已满足任务所需的主题/时间/观点覆盖，或平台出现频率限制。不得因首屏少量结果或单篇正文成功而停止。
   - 候选选择同时覆盖相关性、时间、作者、互动量、内容类型和观点差异；高互动内容、低互动但高度相关内容、正负面或不同使用场景都应保留。稳定 ID 去重，保留每个候选的来源页和排序条件。
   - 多平台任务维护简短台账：对象、目标字段、已尝试路径、已取得证据、页码/游标、错误、状态（已完成/部分完成/暂时阻塞）和下一步。平台返回的总数、实际返回数和已详细读取数分开记录。

4. **平台专用读取路径**
   - **小红书**：保留搜索结果中的 note_id、xsec_token 和完整签名链接。详情、评论和下载优先使用规范的 `/explore/<note_id>?xsec_token=<token>&xsec_source=pc_search` 链接；搜索卡片的 `/search_result/...` 链接出现导航拒绝时，使用同一 note_id 和原始 token 转为规范 `/explore` 路径，禁止猜测或拼接不存在的 token。OpenCLI 的 `note` 读取正文和互动字段，`comments --with-replies` 读取主评论及楼中楼，`download` 读取全部图片；Socai 可用 `search` 发现候选，再用带签名的 `get-notes` 批量读取正文、图片 OCR、评论和回复。图片正文信息不足时按原顺序读取所有图片并做 OCR/视觉核验。
   - **知乎**：TikHub 搜索或 AI 搜索只用于发现问题/回答/文章 ID；使用问题/回答 ID 的结构化接口或 OpenCLI `answer-detail`、`answer-comments` 读取正文和评论。专栏文章使用原文页或下载接口读取全文与图片引用。问题页导航出现 `Navigation rejected` 时转为 ID/API 路径。评论接口返回 `EMPTY_RESULT` 且详情的评论数为 0 时，记录为已核实的零评论；其余情况记录实际返回量与分页范围。
   - **微信公众号**：搜狗结果仅作发现；入选文章必须过滤到 `mp.weixin.qq.com/s/...` 原文。TikHub 稳定链路为：POST `/api/v1/wechat_search/v2/fetch_search`，设置 `business_type=article`、`raw=false`，保存游标并过滤原文；再 POST `/api/v1/wechat_mp/v2/fetch_article_detail`，设置 `raw=false` 读取正文、图片和评论字段。长链接或原端点 HTTP 400 时，按实时接口帮助改用 `fetch_article_detail_h5`。公众号搜索应按账号、关键词和最新排序分别核对，尤其要比较发布时间。传给 OpenCLI 前去除 `#rd` 等片段，保留有效查询参数；以原文正文、作者和发布时间为准。Computer Use 直接打开原文若受站点安全策略限制，改用上述接口或 OpenCLI，不绕过安全策略。
   - **Bilibili**：搜索结果转为 BV 号后读取视频详情、简介、缩略图、互动字段和平台字幕。字幕为空时先用已知有字幕的视频测试工具链，再判断该视频确实无字幕；必要时对允许处理的音频做机器转写并单独标记。OpenCLI 评论请求的主评论上限为 50，使用每条评论的 rpid 和 `--parent <rpid>` 读取楼中楼；显示评论总数不等于已读取数量，必须报告实际主评论、回复和分页范围。TikHub 作为分页/评论回复回退。
   - **YouTube**：搜索限定视频，使用视频 ID 读取标题、频道、发布时间、简介、播放/互动数据、缩略图、平台字幕或转写和评论。OpenCLI 评论入口的请求上限为 100，但实际返回数量可能更少且未必提供总数/游标；记录真实返回量，使用 TikHub 或页面路径补充评论回复和分页。字幕为空时先用已知有字幕的视频区分工具问题与源视频无字幕，再按需机器转写。
   - Windows 命令行传递含 `&` 的完整链接时，必须将整个 URL 作为一个参数；使用原始 URL、ID 和游标继续任务，不用搜索片段代替。

5. **图片、OCR 与媒体路径**
   - 详情数据提供图片 URL 时，按平台展示顺序读取纳入范围的全部图片；记录可见总数、已读取数、OCR 数、失败数和失败原因。分别标记“平台正文文字”“图片内可见文字”“OCR 转写”“视觉推断”和“无法辨认片段”，低清、遮挡、竖排、数字或单位不确定时保留不确定性并人工复核关键数字。
   - 图片 CDN 的 `ERR_BLOCKED_BY_CLIENT`、热链拒绝、空白预览、字段截断和 `fetch failed` 先归类为当前工具/资源路径问题。核对详情接口的原图、备用 URL、签名参数和图片数量，再低频尝试一个独立的浏览器或接口路径；失败路径不能证明原帖没有图片。
   - OpenCLI 的正文页面读取和 Node 图片下载可能使用不同网络路径。若图片下载出现 `connect EACCES` 或同类沙箱网络错误，先复测允许外网的只读执行环境；这类错误优先归因于执行环境，不要求用户重新登录。下载成功后核对本地文件数量、大小、Markdown 引用替换和乱码；用户未要求保存时仍只保留必要的临时证据。
   - 图像读取只支持本次证据核验。OCR 结果不能回写成平台正文的直接引文，也不能用单张图片推断整组图片内容。

6. **评论、互动与覆盖边界**
   - 有评论接口时同时记录平台显示总数（若提供）、实际返回数、分页/游标、主评论与回复数量、筛选条件和抽样依据。评论较多时按相关性、互动量、时间、作者和观点差异抽样，并实际读取文本；“高赞”“热门”只依据真实 likes/replies 等字段。
   - Bilibili 通过 rpid 选择高互动、观点差异明显的评论读取楼中楼；YouTube 优先用可分页的独立接口获取回复；小红书保留主评论与嵌套回复的层级；公众号评论总数为 0 时如实记录。评论数、回复数和已读数始终分开。
   - 只能访问到一部分评论时，结果标记为“评论部分覆盖”，列出已读数量和未覆盖原因；没有总数或继续游标时，不推断平台评论全集已覆盖。

7. **失败恢复顺序与错误解释**
   - 空结果、验证码、超时、403、登录墙、字段缺失或截断只说明当前路径不可用。先保存查询、链接/ID、页码/游标、原始错误和会话状态，再依次执行：
     1. 检查实时帮助、实时 OpenAPI、端点版本/区域域名、必填参数、稳定 ID、网络、余额、登录态和会话占用；只对有明确修正依据的问题复测一次。
     2. 改用至少一种独立的只读路径；必要时用公开 `site:` 搜索发现原始链接，再回到平台原文或结构化接口核实。
     3. 复测原始查询或从保存的 ID/游标继续，并更新台账。
     4. 仍有登录、扫码、验证码、人机验证、双因素认证、浏览器接管或凭据授权需求时，向用户给出具体应用、窗口、操作和仍缺失字段，然后暂停等待。
   - TikHub HTTP 400/404 先核对实时接口和区域域名；`No valid session ID provided` 先检查 MCP 会话并按需低频改用同一官方 REST API，404 不能解释为无结果。微信公众号搜索只有在返回 `no_more` 且结果为空时才记录为正常空结果。OpenCLI 的 `Navigation rejected` 记录为浏览器桥接/路径问题；`fetch failed` 进一步区分 URL、CDN、网络执行环境和登录状态。
   - 出现访问频繁告警、账号异常、IP 限制或平台安全提示时立即停止该自动化路径；不通过换 IP、伪造 Referer/Cookie、绕过验证码、复制 Cookie 或高并发方式规避。其他已确认安全的路径仍可继续处理。

8. **用户接管步骤**
   - **OpenCLI 登录/桥接**：用户启动 OpenCLI 或 Browser Bridge，确认目标 Chrome 配置保持正常登录并保留窗口；我检查 `opencli doctor` 和当前 connected profile。用户无需发送密码、Cookie 或验证码内容。
   - **Socai 登录**：用户在 Socai 管理的 Chrome 窗口中完成平台正常登录，保持该窗口和会话打开，回复“已登录”；我从保存的查询、签名 ID、页码或游标继续。
   - **Computer Use 接管**：用户仅在明确提示后授权当前浏览器/应用操作，保持目标页面可见；遇到验证码或安全确认由用户在平台页面正常完成。用户完成后回复“已完成”，我重新读取页面状态并从断点继续。
   - 登录失效、网络受限和适配器报错分别处理；用户登录不能修复 `Navigation rejected`、CDN 网络沙箱或平台接口分页上限。

9. **完成判定与结果披露**
   - 每个平台分别给出“已完成、部分完成、暂时阻塞”，说明搜索候选数量/页数/游标、实际全文读取对象、图片与 OCR 数量、字幕/转写数量、评论与回复覆盖、未取得字段、已尝试路径、具体错误和所需用户操作。
   - 结果中分开呈现候选发现和实际证据。主要参考资料只列出实际读取过正文、全文、图片、字幕或评论的原始标题、作者/账号、日期和原始链接；搜索卡片、AI 摘要和未打开的链接单独列为候选。
   - 输出正文、图片文字、OCR、视觉推断、平台字幕、机器转写、评论原文和互动数字时标明各自来源；对未覆盖内容、平台不可见内容和工具限制保持明确边界。视频网站以视频外信息为主，不把视频文件本身的下载当作完成条件。

10. **扩展能力与规则维护**
   - 现有 TikHub、Socai、OpenCLI/smart-search 和 Computer Use 能满足常规五平台主检索链路。登录状态、Browser Bridge、图片 CDN 网络路径、平台评论分页和适配器稳定性优先通过上述自检与独立回退处理。
   - 安装或更新能力后先核对导入/版本/日志，执行初始化和工具列表检查，再用一个低风险对象验证正文、图片和评论字段；“工具可调用”不等于“端到端检索完成”。规则、工具版本、会话状态和已知限制出现变化时及时更新本节。
