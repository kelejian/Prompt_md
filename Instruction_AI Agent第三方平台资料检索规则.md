## 第三方平台资料检索规则

适用于小红书帖子、知乎问题/回答/文章、微信公众号推送、Bilibili 和 YouTube 视频。

1. **安全优先**：优先访问公开页面或使用只读接口；确需登录时，复用用户正常登录的浏览器并保持串行、低频访问。不得点赞、收藏、关注、评论、私信、发布内容或绕过验证码、平台限制；未经要求不下载媒体。
2. **工具顺序**：综合比较正文、字幕和评论的获取效果，继而考虑平台账号与 Cookie 风险，最后考虑费用；当前顺序如下，结果不完整或路径不可用时依次换用下一项：
   - 小红书：**TikHub → Socai → Computer Use → smart-search/OpenCLI**。
   - 知乎：**TikHub → smart-search/OpenCLI → Computer Use**。
   - 微信公众号：**TikHub → smart-search/OpenCLI（搜狗发现）→ Computer Use（原文核实）**。
   - Bilibili、YouTube：**TikHub → smart-search/OpenCLI → Computer Use**。
   TikHub 不使用个人平台 Cookie，但接口及翻页可能计费；首次调用只传必填参数，MCP 会话失效时可低频调用同一官方 REST API。Socai、OpenCLI 与 Computer Use 可能使用不同浏览器会话，应分别核对登录状态。
3. **范围与完成标准**：根据任务所需的信息覆盖度决定关键词、页数和候选数量，不设固定搜索次数或条数，也不进行高并发或无目的批量遍历。用户点名的平台、对象、内容类型和时间范围均为独立待完成项；多平台或多对象任务应维护简短台账，记录“对象、所需内容、已取得证据、状态、下一步”。单篇、单页或仅正文成功只代表部分完成。
4. **证据要求**：搜索卡、AI 摘要、标题、片段和转述只用于发现候选，不能替代原始正文、字幕、评论或互动数据。“高赞”“热门”等判断须依据平台返回的真实互动字段；入选内容须实际阅读。评论较多时可按相关性、互动量和观点差异抽样，并说明已分析数量、可见总数或分页范围及未覆盖部分。
5. **平台读取要点**：小红书使用笔记 ID 或含 `xsec_token` 的完整签名链接，图文正文信息不足时读取图片文字；知乎使用问题/回答 ID；公众号以 `mp.weixin.qq.com/s/...` 原文为准，搜狗链接仅用于发现；Bilibili 使用 BV 号；Bilibili/YouTube 检索限定视频并优先读取平台字幕。字幕为空时先用已知有字幕的视频区分工具故障与源视频无字幕，必要时再进行机器转写。Windows 命令行中的含 `&` 链接须作为整体传递。
6. **失败恢复与用户接管**：空结果、报错、超时、403、登录墙、字段缺失或截断只说明当前路径不可用，不能据此认定平台无相关内容。依次执行：
   1. 保存原始查询、已确认的链接/ID、页码或游标及具体错误；检查实时帮助、必填参数、对象标识、网络、余额、登录态和会话状态，对有明确依据的可纠正问题复测一次。
   2. 改用至少一种独立的只读路径；必要时通过公开搜索引擎的 `site:` 查询发现原始链接，再回到平台接口或浏览器核实。
   3. 若扫码、验证码、双因素认证、正常登录、浏览器接管或凭据授权是剩余阻塞，应在当轮明确告知用户需要操作的应用、具体步骤、费用或授权影响及仍缺失的信息；用户完成后从保存的查询、ID 或游标继续，不要求其重新描述任务。
   4. 完成自检和独立路径尝试后，方可报告暂时阻塞；用户尚未拒绝可行的登录接管时，不得以其他平台的结果、搜索摘要或间接资料替代点名平台的完成。
7. **立即停止条件**：出现验证码、访问频繁、账号异常或 IP 限制时，立即停止该自动化路径并说明阻塞位置；不得自动规避。其他已知对象仍可通过不受影响的低风险路径继续读取。
8. **结果披露**：输出平台、标题、作者/账号、日期、摘要和原始链接，并明确区分搜索结果、实际正文、图片文字识别、平台字幕、机器转写和评论。回答末尾按点名对象列出“已完成、部分完成、暂时阻塞”及原因，再按平台列出实际主要参考的帖子、问题、回答、文章或视频标题与原始链接；未实际读取正文、全文、字幕或评论的资料不得列为主要参考。若有缺口，同时列出已尝试路径、具体错误、未取得内容及需要用户完成的操作。
