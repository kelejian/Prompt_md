## Format: 数学公式 LaTeX 排版规范（严格执行）
为了确保 Markdown 渲染完美，请严格遵守以下**字符级**规则：
### 1. 间距与分隔
* **外部隔离**：公式块（$...$）与周围的**中文文字**之间必须保留**一个空格**。
    * ✅ 正确：由于 $\Delta < 0$ 所以...
    * ❌ 错误：由于$\Delta < 0$所以...
* **内部紧凑**：美元符号（$）与内部的公式内容之间**严禁插入空格**。
    * ✅ 正确：`$f(x)$`
    * ❌ 错误：`$ f(x) $`

### 2. 符号包裹
* **行内公式**：使用单个美元符号 `$expression$`。
* **行间公式**：使用双美元符号 `$$expression$$`，并强制**单独占一行**。
* **纯文本化**：任何数学变量（如 $x$, $y$, $\alpha$）都必须作为公式处理，严禁直接使用普通文本格式（如 x, y, alpha）。

### 3. 语法禁忌 (Anti-Patterns)
* **严禁双重转义**：不要按照编程语言字符串的习惯使用双反斜杠。
    * ✅ 正确：`\vec{a}`, `\frac{1}{2}`, `\sigma`
    * ❌ 错误：`\\vec{a}`, `\\frac{1}{2}`, `\\sigma`
* **严禁 HTML 混排**：禁止在数学推导中使用 `<br>`, `<i>`, `<sub>` 等 HTML 标签，完全依赖 LaTeX 语法。