# 通用 PPT 制作规范

## 0. 适用原则与优先级

1. 本文件同时适用于两类任务：仅生成可供用户自行排版的 PPT 内容稿；在本地生成内容、格式与视觉效果均符合要求的 `.pptx` 文件。
2. 用户当前指定的任务类型、主题、听众、用途、模板、页数、篇幅、信息密度、输出范围、文件格式和特殊风格要求优先。用户只调整部分事项时，其余内容继续遵守本文件及用户的通用指令。
3. 第 1 部分是两类任务共同遵守的内容要求；仅生成内容稿时执行第 2 部分；需要在本地生成 `.pptx` 文件时同时执行第 1、3 部分。

# 1. 通用 PPT 内容生成要求

## 1.1 任务范围与内容依据

1. 严格遵守当前任务指定的主题、听众、用途、篇幅、页数和输出范围。用户要求某一部分时，只处理该范围，不提前展开其他部分。
2. 内容应严格基于用户提供的资料或明确采用的可靠来源。严禁编造事实、数据、结论、案例、实验结果和图表；不得擅自改变原始资料中的概念、数据含义、结论强度和适用范围。应在 PPT 正式内容外的地方（比如对话回答中、PPT内容稿的正文节之后等处），说明清楚：内容依据/参考资料/参考来源。
3. 应区分资料直接支持的事实、基于资料所作的分析以及仍需确认的事项。信息不足时，在可交付页面内容之外单列待确认事项；不得把“待核验”“待确认”“待补充引用”等过程提示写入正式页面或讲稿。只有用户明确要求保留占位标记时，才可在非最终版本中醒目标注。
4. 用户提供参考 PPT 或模板时，应先分析其页面结构、标题风格、信息密度、图文比例、配色和视觉元素，再组织内容。内容和表达风格应与模板及任务场景保持一致。
5. PPT 内容必须逻辑自洽，严禁自相矛盾。不得加入资料无法支持的内容；避免加入超出当前汇报目的、可能引起无必要追问、质疑或争议的过细信息。对用户明确要求排除的内部信息、敏感信息和讨论细节，应完全排除。

## 1.2 大纲、主线与页面组织

1. 先思考 PPT 的文字内容大纲和相应的视觉呈现思路，再展开逐页内容。用户只要求大纲时，不提前生成全部页面正文。
2. 注意 PPT 页与页之间、页面内容之间的逻辑关联与连贯性，围绕一条清晰主线连续推进，使前一页自然引出后一页，避免做成互不相关的信息块。
3. 标题、章节和分点的层级应与内容的语义层级一致。同级页面采用一致的分类依据，避免把背景、问题、方法、流程步骤、结果和建议混在同一层级。
4. 目录、每页页顶标题、页内标题、中心句和关键要点应使用自然、准确且信息完整的表达，明确揭示本页的对象、关系或结论。标题类文字不得过分直白简略；页顶标题和页内标题的字数不能太少，应自然连贯。正文表述同样应保持自然、完整，禁止过度压缩或用碎片化短词句拼凑内容，也不得使用“背景介绍”“结果分析”等信息单薄、无法说明具体内容的笼统标题。
5. 关键技术架构、方法思路和方案内容应逻辑清晰、层次完整，保留足以使目标听众理解其含义和作用的必要细节。任务属于详细进展汇报或文档报告型 PPT 时，不应机械套用极度精简的要点式写法，也不得因追求简短而省略关键条件、步骤关系和解释。
6. 内容详略应根据听众背景、汇报时长、模板容量和用户指定的信息密度确定。听众对相关领域不熟悉时，应补充理解主线所必需的背景和概念；过细、过复杂且难以在汇报时间内消化的内容不应放入主体页面。

## 1.3 语言、术语与可展示性

1. 使用平实、严谨、自然的学术或专业语言，避免浮夸的 AI 语言风格、宣传式表述、主观夸赞、空泛升华和语义过重的结论。
2. 禁止写没有实际信息价值的废话。不要硬做比喻，不要生搬硬套，不要牵强附会；避免过于口语化、叙述琐碎或需要听众猜测所指的表达。
3. **低价值否定式纠偏一律禁止。**不得自行构造用户、资料或听众未提出的错误理解，再用“不是 X，而是 Y”“不能只看 X”等立靶式表述加以否定。需要纠正明确错误时，直接说明错误、依据和正确内容。
4. 概念、定义、对象、变量和称谓必须前后一致。同一对象不得为避免字面重复而频繁更换名称；专业术语、专有名词和英文缩写首次出现时，应给出规范中文名称和必要解释。
5. 正式页面和讲稿中禁止出现任何“指令性文字”，也不得出现任务安排、制作过程、内部讨论、提示词内容或不方便向听众展示的信息，例如“本次汇报定位……”“此处不展示……”等面向制作者而非听众的说明。
6. 行文立场应符合汇报者身份和任务场景，执行主体明确。页面内容应能够由汇报者自然理解和口头讲解，不采用叙述者身份悬空的顾问式或上级布置式口吻。
7. 讲稿、补充说明、注意事项、提醒点、资料核对信息等非 PPT 正式展示内容，不得混入页面正文；需要随 PPT 保留时，应放在对应页面下方的备注区中，避免在放映时直接展示给听众。

## 1.4 单页内容与图表安排

1. PPT 每一页的文字内容应尽可能充分，多一些总比少一些要好，**宁多勿少**，因为用户自行删减和简化通常比自行补充内容更容易。具体每页字数及信息密度必须参考用户提供的示例或参考 PPT；同时结合该页是否包含统计图表、流程图、框架图、公式、图片等非文字素材，相应安排文字量。
2. 分点数目不宜过多；页面确需多级分点时，通常控制在 2—3 级。连贯内容不应机械拆成大量碎片化列表，列表层级和数量以能够清楚呈现页面逻辑为准。
3. 页面加入统计图表、流程图、框架图、公式或大幅图片后，应根据其所占空间调整文字量。公式中的符号、变量和页内说明应前后一致，并解释公式与本页主旨之间的关系。
4. 图表应服务于明确的信息表达，优先复用用户资料中已有、来源明确且能够核对的素材。需要制作或调整图表时，应先提炼需要表达的数据关系、概念层级、流程或因果关系，再选择相应形式。
5. 图表标题和较长的解释文字宜由 PPT 中独立、可编辑的文本框承载，避免将可复用图表与特定页面的说明永久合并。图表素材及其 Excel、PPT、SVG、绘图代码等可编辑源文件应保存在合适位置，便于后续复用和用户自行修改，同时避免冗余保存。

# 2. 仅生成内容符合要求的 PPT 内容稿的 Prompt

请仅生成 PPT 内容稿，不创建或修改 `.pptx` 文件。严格遵守第 1 部分的通用内容要求，先规划大纲，再按页给出页码、页顶标题、可直接放入页面的完整文字内容及必要的图表或公式建议；只有用户明确要求时才补充讲稿、备注或排版说明。直接输出当前要求的范围，不添加确认式开场、流程预告和无信息量的总结收尾。

# 3. 在本地生成内容与格式均符合要求的 PPT 文件（.pptx）的 Prompt

请在本地生成内容、格式和视觉效果均符合要求的 `.pptx` 文件，并将“能够在 Microsoft PowerPoint 中自由二次编辑”作为强制验收目标。除第 1 部分外，还必须遵守以下要求。

## 3.1 执行顺序、模板与文件管理

1. 开始制作前，先读取当前项目适用的 `AGENTS.md` 及用户指定的参考资料、模板和输出要求；通过只读检查了解项目目录结构、现有文件及命名方式。
2. 先生成或确认每一页的完整内容稿和视觉设计方案，再进入正式制作。不得在内容主线、页数规划和页面用途尚未明确时直接堆叠版面元素。
3. 用户提供 PPT 模板时，应在模板副本上制作，严格继承其幻灯片尺寸、主题风格、首尾页、背景、固定标识、配色和主要视觉语言。不得覆盖或修改原始模板。
4. 最终 PPT、可编辑图表素材及必要中间工作文件应保存到与任务和项目结构相符的位置。中间文件与最终交付文件应明确区分，不得散落在原始资料目录中，也不得生成含义不明或内容重复的副本。
5. 最终交付至少包含用户要求的 `.pptx` 文件。讲稿、补充说明、注意事项、提醒点和资料核对信息等非正式内容应写入对应页面下方的备注区，不得放在正式页面中。用户要求来源核对版本时，应优先将数据和事实的具体来源位置写入对应页面的备注区，并另行输出不含核验提示和内部注释的 clean 版本。
6. 可根据当前任务需要使用 PPT Master，以及可用的 PPT 制作、图像转可编辑幻灯片等 skills 或 plugins，例如 `codex-ppt`、`image-to-editable-ppt`。这些工具均为按需选项，应根据模板复用、视觉生成、对象级编辑和质量检查等实际需要选择，不要求为形式完整而全部调用。
7. PPT Master 的本地完整仓库路径为 `E:\ppt-master`。需要调用时，应先阅读其中 `skills/ppt-master/SKILL.md` 及与当前任务直接相关的说明；该仓库仅可读取和执行，不得修改、覆盖或删除其中的文件，也不得将其工具代码复制到当前项目。其 Python 脚本使用合适的conda 环境；当前任务的输入材料、中间文件和最终 PPT 仍应保存在当前项目的合适目录中。

## 3.2 版式、字体与视觉体系

1. 中文字体默认使用**微软雅黑**，英文字体默认使用 **Arial**。PPT 中使用的字号大小必须为偶数。
2. 各页页内正文内容使用的字号种类建议控制在 3—5 种，常用字号包括 16、18、20、24 等。非标题类正文主要使用 16 号，必要时可使用稍小的 14 号；24 号及更大的字号主要用于中心句、核心结论或页内标题。相同层级的文字应使用一致字号，避免字号、字体、加粗和颜色五花八门。加粗使用应合理，不要对所有的普通正文内容均加粗。
3. 页顶标题可以使用 30 号以上的偶数字号，封面大标题可以使用 40 号以上的偶数字号。页内补充的注释类文字，例如“参考 ××× 研究”等，可以使用 12 号及以下的偶数字号。
4. 整套 PPT 应保持统一的字体、配色、线条、圆角、图标和间距体系；每页可根据内容采用不同的图文结构和视觉元素，避免机械复用同一中间页布局或使所有页面排版完全相同。
5. 排版应围绕本页核心信息建立清晰的视觉层级和视觉重心。合理处理图文比例、对齐、留白和对象间距，避免页面过空、局部拥挤、元素失衡或装饰喧宾夺主。
6. 所有可展示文字必须属于正式内容。页面中不得残留设计说明、占位文字、文件路径、内部批注、提示词、对象名称或其他制作痕迹。

## 3.3 PowerPoint 可编辑性

1. 每个文本框只保留一层明确的文字格式。写入或替换文字前，应清除原有字符级、段落级和占位符继承格式，确保字号、字体、加粗、颜色、对齐、行距等属性在文字片段、段落默认格式和文本框格式之间不存在冲突。需要不同样式的内容应拆分为独立文本框。
2. 禁止使用自动缩排、自动字号缩放、形状适应文字和行距压缩。文字放不下时，应调整文本框尺寸、文字内容或页面布局。
3. 正文内容使用幻灯片层的普通文本框和形状。母版与占位符仅用于固定模板背景、页码和装饰，不得把需要用户修改的正文内容隐藏在母版或复杂占位符结构中。
4. 删除所有限制编辑的对象属性，包括禁止组合、禁止选择、禁止移动、禁止缩放和禁止编辑字体格式等锁定设置。
5. 需要整体移动的模块使用普通形状和文本框构成，并预先测试组合与取消组合。图表、表格和占位符按特殊对象单独处理，不得默认其与普通形状具有相同的组合能力。
6. 文本、示意图、流程图和框架图不得栅格化；框、线、箭头、节点和主要视觉对象应尽可能保持独立可编辑。统计图表应尽可能保留底层数据或相应的 Excel、PPT、SVG、绘图代码等源文件。
7. 所有对象应使用唯一且含义明确的名称，禁止重名、无用途的隐藏对象、失效占位符和无法识别用途的残留元素。

## 3.4 生成后的逐页检查与交付

1. 生成完成后必须渲染并逐页进行视觉检查，至少检查：文字溢出与截断、对象遮挡、边界越界、对齐、留白、间距、字体与字号一致性、图表坐标、数据标签、箭头关系及页面视觉重心。
2. 检查 PPT 中的事实、数据、排序、标签、单位、公式符号和图中数字是否与输入资料完全一致；已确认的数据图不得在排版修改过程中重新计算或替换数据。
3. 对发现的问题应在保持现有整体视觉体系和已稳定页面布局的前提下进行定点修改。不得因局部问题无必要地重做其他页面；修改后应重新执行相关页面的视觉检查。
4. 最终导出的 `.pptx` 应能够在 Microsoft PowerPoint 中正常打开、编辑、移动、缩放、组合与取消组合主要对象。交付前应实际检查文件可打开性、页面数量、备注内容、字体设置、对象可编辑性以及最终版本中是否残留非正式注释。
